{
  "name": "b3",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": ["server", "ui"],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.0.0"
  }
}
